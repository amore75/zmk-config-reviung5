# SPDX-License-Identifier: MIT

# Display support (driver is selected via devicetree)
CONFIG_ZMK_DISPLAY=y

# Sleep support
CONFIG_ZMK_SLEEP=y

# External power rail support (matches ext_power node in the board overlay)
CONFIG_ZMK_EXT_POWER=y
